import styled from "styled-components";
import { theme } from "../theme";

export const Main = styled.div`
  margin: 0 auto;
  padding: 0 ${theme.defaultSpace};
  max-width: ${theme.mainMaxWidth};
`;
